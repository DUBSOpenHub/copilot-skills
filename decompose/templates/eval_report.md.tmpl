# Eval Report — `decompose` Skill

**Run ID:** {{RUN_ID}}
**Run at:** {{RUN_AT}}
**Skill version:** {{SKILL_VERSION}}
**Fixtures evaluated:** {{FIXTURE_COUNT}}
**Evals run:** {{EVAL_COUNT}}
**Total records:** {{TOTAL_RECORDS}}

---

## 1. Summary Table

> ✅ PASS · ❌ FAIL · ⚠️ PARTIAL

| Eval | {{#each FIXTURE_IDS}}{{this}} | {{/each}}Pass Rate |
|---|{{#each FIXTURE_IDS}}---|{{/each}}---|
{{#each EVALS}}
| {{this.eval_id}} | {{#each this.fixture_results}}{{#if this.pass}}✅{{else}}❌{{/if}} | {{/each}}{{this.pass_rate}} |
{{/each}}

**Overall pass rate:** {{OVERALL_PASS_RATE}}
**Critical failures (must-be-100% evals):** {{CRITICAL_FAILURES}}

---

## 2. Fixture-Level Results

{{#each FIXTURES}}
### Fixture: `{{this.fixture_id}}`

**Role:** {{this.role}} · **Input summary:** {{this.input_summary}}

| Eval | Pass | Score | Threshold | Notes |
|---|---|---|---|---|
{{#each this.eval_results}}
| {{this.eval_id}} | {{#if this.pass}}✅{{else}}❌{{/if}} | {{this.score}} | {{this.threshold}} | {{this.notes}} |
{{/each}}

{{#if this.has_diffs}}
**Diffs from golden:**
```
{{this.diffs}}
```
{{/if}}

{{/each}}

---

## 3. Schema Validation Findings

**Genome schema validation:** {{SCHEMA_VALIDATION_SUMMARY}}

{{#each SCHEMA_FINDINGS}}
- **Fixture:** `{{this.fixture_id}}` · **Result:** {{#if this.pass}}✅ Valid{{else}}❌ Invalid — {{this.error}}{{/if}}
{{/each}}

**DAG semantic validation:**

{{#each DAG_FINDINGS}}
- **Fixture:** `{{this.fixture_id}}` · **Rule:** {{this.rule}} · **Result:** {{#if this.pass}}✅{{else}}❌ {{this.detail}}{{/if}}
{{/each}}

---

## 4. Redaction Findings

**Seeded secrets audited:** {{REDACTION_SEED_COUNT}}
**Secrets found in artifacts:** {{REDACTION_FINDINGS_COUNT}}

{{#if REDACTION_CLEAN}}
✅ Zero seeded secrets found in any written artifact.
{{else}}
❌ The following seeded secrets were found in artifacts:

| Fixture | Artifact | Secret pattern | Location |
|---|---|---|---|
{{#each REDACTION_VIOLATIONS}}
| {{this.fixture_id}} | {{this.artifact}} | {{this.pattern}} | {{this.location}} |
{{/each}}
{{/if}}

---

## 5. Agent-Readiness Score

**Rubric:** `eval/rubrics/agent_readiness.md`
**Threshold:** 0.80 (80%)

| Fixture | Score | Pass |
|---|---|---|
{{#each AGENT_READINESS_SCORES}}
| {{this.fixture_id}} | {{this.score}} | {{#if this.pass}}✅{{else}}❌{{/if}} |
{{/each}}

**Aggregate score:** {{AGENT_READINESS_AGGREGATE}}

---

## 6. Accessibility Score

**Rubric:** `eval/rubrics/accessibility.md`
**Threshold:** 0.85 (85%) for non-technical role fixtures

| Fixture | Role | Score | Pass |
|---|---|---|---|
{{#each ACCESSIBILITY_SCORES}}
| {{this.fixture_id}} | {{this.role}} | {{this.score}} | {{#if this.pass}}✅{{else}}❌{{/if}} |
{{/each}}

**Aggregate score (non-technical fixtures only):** {{ACCESSIBILITY_AGGREGATE}}

---

## 7. Recommended Fixes

{{#if NO_FAILURES}}
✅ No failures detected. All evals passed at required thresholds.
{{else}}
{{#each RECOMMENDED_FIXES}}
### Fix {{@index_plus_one}}: {{this.title}}

- **Failing eval:** `{{this.eval_id}}`
- **Failing fixtures:** {{this.fixture_ids}}
- **Root cause:** {{this.root_cause}}
- **Recommended fix:** {{this.fix}}
- **Priority:** {{this.priority}}

{{/each}}
{{/if}}

---

*Report generated by `decompose` eval runner · Template: `eval_report.md.tmpl` v{{SKILL_VERSION}}*
*Sealed eval results are separate from this open report.*
