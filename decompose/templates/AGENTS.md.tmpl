# AGENTS.md — Execution Pack for `{{GENOME_ID}}`

> Generated by `decompose` skill v{{SKILL_VERSION}} · Session `{{SESSION_ID}}` · {{CREATED_AT}}
> **Do not modify this file manually.** Regenerate via `decompose --replay replay.jsonl`.

---

## Role

This plan was built for: **{{ROLE}}**

---

## Goal

{{INTENT_ONE_SENTENCE}}

---

## Nodes

The following work nodes make up this plan. Execute in order unless marked as parallelizable.

{{#each NODES}}
### {{@index_plus_one}}. {{this.title}} (`{{this.id}}`)

- **Type:** {{this.type}}
- **Description:** {{this.description}}
- **Owner:** {{this.owner_hint}}
- **Human Gate:** {{#if this.human_decision_gate}}⛔ YES — Do not proceed past this node without explicit human approval.{{else}}No{{/if}}
- **Confidence:** {{this.confidence_pct}}%
- **Risk:** {{this.risk}}
- **Prompt template:** `{{this.prompt_template}}`
- **Acceptance criteria:**
{{#each this.acceptance_criteria}}
  - {{this}}
{{/each}}
{{#if this.unknowns}}
- **Open unknowns at this node:**
{{#each this.unknowns}}
  - {{this}}
{{/each}}
{{/if}}

{{/each}}

---

## Human Gates

The following nodes require explicit human approval before successor nodes may proceed. Agents must stop and wait.

{{#if NO_HUMAN_GATES}}
None — this plan has no human decision gates.
{{else}}
{{#each HUMAN_GATE_NODES}}
- **`{{this.id}}`** — {{this.description}}
{{/each}}
{{/if}}

---

## Do Not Assume

The following items are explicitly unknown. Agents must NOT resolve these silently — surface them to a human.

{{#if NO_UNKNOWNS}}
No unresolved unknowns. All key facts were established during planning.
{{else}}
{{#each DO_NOT_ASSUME}}
- {{this}}
{{/each}}
{{/if}}

---

## Start Here

Begin execution at node **`{{START_NODE_ID}}`**: {{START_NODE_TITLE}}

**Prompt for first node:**

> {{START_NODE_PROMPT_EXCERPT}}

---

## Edges (Dependency Map)

```
{{#each EDGES}}
{{this.from}} --[{{this.type}}]--> {{this.to}}{{#if this.note}} ({{this.note}}){{/if}}
{{/each}}
```

---

## Open Questions

These questions were not resolved during planning and require human input before or during execution:

{{#if NO_OPEN_QUESTIONS}}
None.
{{else}}
{{#each OPEN_QUESTIONS}}
{{@index_plus_one}}. {{this}}
{{/each}}
{{/if}}

---

## Session Replay

To reconstruct this session or regenerate artifacts:
```
decompose --replay replay.jsonl
```

---

*Genome ID: `{{GENOME_ID}}` · Schema: `{{SCHEMA_VERSION}}`*
